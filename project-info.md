# Notes on Collaboration

1. Compile with at least the following flags: gcc -Wall -Werror -Wpedantic foo.c
2. Give executables the `.out` extension so they get gitignored
3. I use snake_case instead of camelCase for all var/method naming, lmk if you want to change it
